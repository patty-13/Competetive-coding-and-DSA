#include "iostream"
#include "vector"
#include "algorithm"
#include "map"
#include "math.h"
#include "string"
#include "stack"
using namespace std;
typedef long long ll;
int main()
{
    // ios_base::sync_with_stdio(false);
    // cin.tie(NULL);
    //int t,a,b,c,d;
    int t,n,m;
    cin>>t;
    while(t--)
    {
     
        // cin>>a>>b>>c;
        // d=0;
        // if(c-b == b-a && c -a >1)
        // {
        //     d = c - a;
        // }
        // else if(c-b == b-a)
        // {
        //     d = c + 1;
        // }
        // cout<<d<<"\n";
        //Problem 2
        ll a[1000000000][1000000000];
        cin>>n>>m;
        for(int i = 0 ; i < n ; i++)
        {
            for(int j = 0 ; j < m ; j++)
            {
                cin>>a[i][j];
            }
        }
        for(int i = 0 ; i < n ; i++)
        {
            for(int j = 0 ; j < m ; j++)
            {
                

            }
        }       
    }
}