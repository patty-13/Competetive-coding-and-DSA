#include "iostream"
#include "unordered_map"
#include "algorithm"
#include "map"
#include "vector"
#include "string"
#include "math.h"
#include "cstring"
#include "stack"
#include "queue"
using namespace std;
int main()
{
    // solution 1
    // int t;
    // string s;
    // cin>>t;
    // while(t--)
    // {
    //     vector<char> a(3);
    //     for(int i=0; i< a.size() ;i++)
    //     {
    //         cin>>a[i];
    //     }
    //     sort(a.begin(),a.end());
    //     for(int i = 0 ;i < a.size() ; i++)
    //     {
    //         if(a[i] == a[i+1])
    //         {
    //             s = "NO";
    //             break;
    //         }
    //         else
    //         {
    //             s = "YES";
    //         }
    //     }
    //     cout<<s<<"\n";
    // }

    // solution 2
    //  int t,n,s;
    //  cin>>t;
    //  while(t--)
    //  {
    //      cin>>n;
    //      vector<string> a(n);
    //      for(int i =0 ; i < n ; i++)
    //      {
    //         cin>>a[i];
    //      }
    //      for(int i =0 ; i <n ; i++)
    //      {
    //         if(a[0].compare("stop")==0)
    //         {
    //              s = 404;
    //              break;
    //         }
    //         else if(a[i] == a[i+1] && a[i].compare("stop")==0)
    //         {
    //             s = 404;
    //             break;
    //         }
    //         else 
    //         {
    //             s = 200;
    //         }
    //      }
    //      cout<<s<<"\n";
         
    //  }

    // solution 3
    int t, n,k;
    cin>>t;
    while(t--)
    {
        cin>>n>>k;
        vector<int> a(n);
        for(int i = 0; i < a.size() ; i++)
        {
            cin>>a[i];
            vector<int> a(n);
            sort(a.begin(),a.end());
            for(int 0,;)
            {
                
            }
        }
    }



}